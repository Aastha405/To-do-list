{
  "name": "ToDoList-Smart-Contract",
  "version": "1.0.0",
  "description": "A Solidity-based ToDo List Smart Contract using Hardhat",
  "scripts": {
    "test": "hardhat test",
    "deploy": "npx hardhat run scripts/deploy.js --network coreTestnet2"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "hardhat": "^2.20.0"
  }
}
